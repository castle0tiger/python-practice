1번 / from langchain.tools import tool
langchain.tools 이라는 모듈에서 tool 이라는 도구 하나만 사용할려고 불러올기


2번 / llm = ChatGroq(model="llama-3.3-70b-versatile", api_key=os.getenv("GROQ_API_KEY"))
ChatGroq 클래스로 찍어낸 객체를 llm에 담는다는 뜻이며, Grog의 "llama-3.3-70b-versatile" 모델을 사용,
해당 모델 API 연결 키는 env 파일에서 GROQ_API_KEY 라고 정의된 것을 가져와다 연결


3번 / @tool
def calculator(expression: str) -> str:

@tool -> 데코레이션으로 아래의 정의된 calculator 함수가 Qtool에 등록되며, 등록시에 배달된다.  

(expression: str) -> str : calculator 함수의 재료는 str 문자열이며 함수로 돌려주는 값도 str 문자열이다라는 뜻 


4번 / """수학 계산을 수행합니다. 예: '15 * 3', '100 + 50'"""
3번과 연결해서 설명하면, 

@tool                                   
def calculator(expression: str) -> str:
"""수학 계산을 수행합니다. 예: '15 * 3', '100 + 50'"""

tool에 아래의 calculator 함수를 등록한다.
calculator 함수의 """수학 계산을 수행합니다. 예: '15 * 3', '100 + 50'"""는 모두 str 문자열이다. 


5번 / 
    client = chromadb.PersistentClient(
        path=os.path.join(os.path.dirname(__file__), "..", "15_chromadb", "chroma_db")
    )

오른쪽의 실행 결과를 왼쪽에 담는다. 
15_chromadb 파일 안 chroma_db 파일이 있는 위치에서 chromadb라는 객체를 PersistentClient라는 클래스로 찍어내고,
그걸 client에 담는다?


6번 / 
    embedding_fn = embedding_functions.SentenceTransformerEmbeddingFunction(
        model_name="paraphrase-multilingual-MiniLM-L12-v2"
    )

임베딩 하는 것
SentenceTransformerEmbeddingFunction(
        model_name="paraphrase-multilingual-MiniLM-L12-v2"
    )     ---------------> 다국어(한국어, 중국어 등등)로 임베딩할 경우 사용하는 클래스
이걸로 찍어낸 객체를 embedding_fn에 담아낸다는 의미


7번 / results = collection.query(query_texts=[query], n_results=2)
collection에서 함수 query를 실행 : [query] -> query_texts에 담으며, n_results=2 -> 2개의 후보까지 담아낸다. 
이렇게 실행한 값을 results에 담아낸다.


8번 /  return "\n".join(results["documents"][0])
results의 "documents"의 첫번째 값을 리턴한다. 


9번 / tools = [calculator, search_company_rules]
3, 4번 문제와 연결해서 보면, tools에 등록시킨 함수들이 리스트로 들어가 있는 것을 보여준다. 
이를 반복문으로 돌리면 하나씩 순서대로 함수가 실행될 수 있다.


10번 / agent = create_react_agent(llm, tools, prompt="한국어로만 답해...")
에이전트 AI 생성하는 함수로서 

llm = ChatGroq(model="llama-3.3-70b-versatile", api_key=os.getenv("GROQ_API_KEY"))
tools = [calculator, search_company_rules]
연결한다는 것으로 보임. 
prompt="한국어로만 답해..." -> 시스템 프롬프트로 agent의 답변을 제어한다. 



## 오답 다시 답변

3번 : 등록되는 거야. 이건 실행이 아니라, 등록하는 순간 자동으로 배달

4번 : AI가 """~~~""" 안에 있는 내용을 읽고 어떤 도구를 쓰는 판단

5번 : 솔직히 잘 모르겠는데, 생각해보면, 줄바꿈없이 쭉 늘어져서 리턴된다?